function test(name){
    if(name == "haseeb")
    return true;
else 
return false;//this can also be written as following
}

const testa= name => (name=="haseeb" ? true:false);//this is  functio with   input name

console.log(testa("ali"));